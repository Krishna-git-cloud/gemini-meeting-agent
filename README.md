# Gemini Meeting Agent

This is a lightweight AI agent powered by the Gemini 2.5 Flash model. It takes messy meeting notes and outputs structured JSON containing:
1. Key Insights
2. Action Items
3. A polished draft email

## Setup Instructions

1. **Install dependencies:**
   Make sure you have Python installed, open your terminal/command prompt in this folder, and run:
   ```bash
   pip install -r requirements.txt